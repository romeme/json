group 'ru.romeme.json'
version '0.1'

apply plugin:'java'

buildscript {
    repositories {
        mavenCentral()
    }
}

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile project(":core")
    compile project(":fn")
}